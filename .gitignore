Nothing should be ignored since both added files are HTML source files (.html), which are not build artifacts, dependencies, or temporary files. They are source code files that should be tracked in version control.node_modules/
.next/
dist/
build/
*.log
node_modules/
